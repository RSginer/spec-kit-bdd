# vitest-react-todo Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-07-15

## Active Technologies

- TypeScript 5 (`strict` mode), React 19, targeting ES2022 + React 19 + Vite 8 (app); Vitest 4 + `@amiceli/vitest-cucumber` (BDD feature tests); `@testing-library/react` + `@testing-library/user-event` (component-level assertions); `jsdom` (test environment) — all fixed by the project constitution's Additional Constraints (001-weekly-task-list)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5 (`strict` mode), React 19, targeting ES2022: Follow standard conventions

## Recent Changes

- 001-weekly-task-list: Added TypeScript 5 (`strict` mode), React 19, targeting ES2022 + React 19 + Vite 8 (app); Vitest 4 + `@amiceli/vitest-cucumber` (BDD feature tests); `@testing-library/react` + `@testing-library/user-event` (component-level assertions); `jsdom` (test environment) — all fixed by the project constitution's Additional Constraints

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
