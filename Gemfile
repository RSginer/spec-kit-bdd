source "https://rubygems.org"

# Pins the same Jekyll/plugin versions GitHub Pages builds with, so a local
# `bundle exec jekyll serve` matches what gets published.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer ships webrick, which Jekyll's dev server needs.
gem "webrick"
